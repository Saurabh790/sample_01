@Query("SELECT e FROM CaEmailsT e " +
       "WHERE e.emailType = :reminderType " +
       "AND e.remDt <= CURRENT_TIMESTAMP " +
       "AND e.sentFlag = 'N'")
List<CaEmailsT> findTodaysReminders(@Param("reminderType") String reminderType);

CaEmailsRepository
