<netty.version>4.1.130.Final</netty.version>
This will bump all Netty modules (including netty-codec-http) together and remove the CVE.

Then verify:

bat
Copy code
mvn -q dependency:tree -Dincludes=io.netty | findstr /I netty-codec-http
