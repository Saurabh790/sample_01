package com.optum.fads.pgp.behavior.api;

import static org.junit.jupiter.api.Assertions.assertDoesNotThrow;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class FadsPgpBehaviorApiApplicationTest {

    @Test
    void contextLoads() {
        // If the Spring context starts without exceptions, this passes.
    }

    @Test
    void mainMethod_runsWithoutException() {
        assertDoesNotThrow(() ->
                FadsPgpBehaviorApiApplication.main(new String[] {})
        );
    }
}
